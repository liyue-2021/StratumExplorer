# 工具脚本

| 路径 | 说明 |
|------|------|
| `build/build_oilpro.cmd` | Debug 构建 |
| `build/build_release.cmd` | Release 构建 |
| `build/logs/` | 构建日志（gitignore） |
| `verify_environment.ps1` | Qt / CMake / MSVC 环境检查 |

在仓库根目录执行：`cmd /c tools\build\build_oilpro.cmd`
